name: kioclip_flutter
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

workspace:
  - packages/app
  - packages/components
  - packages/data
  - packages/domain

# 開発時に必要なパッケージをここに記述する
dev_dependencies:
  flutter_lints: ^5.0.0
  melos: ^7.3.0
